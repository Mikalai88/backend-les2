export type BlogParams = {id: string}

export type CreateBlogInputModel = {
    name: string
    description: string
    websiteUrl: string
}

export type UpdateBlogInputModel = {
    name: string
    description: string
    websiteUrl: string
}